databases:
  ops:
    dbname: ${OPS_DBNAME}
    user: ${OPS_USER}
    password: ${OPS_PASSWORD}
    host: ${OPS_HOST}
    port: ${OPS_PORT}
  erp:
    dbname: ${ERP_DBNAME}
    user: ${ERP_USER}
    password: ${ERP_PASSWORD}
    host: ${ERP_HOST}
    port: ${ERP_PORT}
  erae:
    dbname: ${ERAE_DBNAME}
    user: ${ERAE_USER}
    password: ${ERAE_PASSWORD}
    host: ${ERAE_HOST}
    port: ${ERAE_PORT}